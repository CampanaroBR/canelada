# Performance

## Rules

- Never use transition: all — specify exact properties
- will-change only for transform, opacity, filter
- Never use will-change: all
- Only add will-change when you notice first-frame stutter
